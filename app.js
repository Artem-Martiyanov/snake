const gameField = document.getElementById('canvas')const ctx = gameField.getContext('2d')const FIELD = {    WIDTH: 400,    HEIGHT: 400,}const CELL = {    SIZE: 20,    STROKE: 2,    COLOR: '#333',}let gameTick = 100gameField.width = FIELD.WIDTHgameField.height = FIELD.HEIGHTclass Snake {    constructor({SIZE = 10, STROKE = 1, COLOR}, field) {        this.cellSize = SIZE        this.cellStroke = STROKE        this.cellColor = COLOR        this.field = field    }    dX = 0    dY = 1    head = {x: 20, y: 20}    tail = []    move() {        this.head.x += this.dX * this.cellSize        this.head.y += this.dY * this.cellSize    }    isOver() {        if (this.head.x >= this.field.WIDTH) {            this.head.x = 0        }        if (this.head.y >= this.field.HEIGHT) {            this.head.y = 0        }    }    render() {        ctx.beginPath()        ctx.rect(this.head.x, this.head.y, this.cellSize - this.cellStroke, this.cellSize - this.cellStroke)        ctx.fillStyle = this.cellColor        ctx.fill()    }}const Snake1 = new Snake(CELL, FIELD)setInterval(() => {    clearField(gameField)    Snake1.isOver()    Snake1.render()    Snake1.move()}, gameTick)function clearField(canvas) {    canvas.width = canvas.width}